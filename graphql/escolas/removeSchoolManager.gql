mutation removeSchoolManager($parentId: ID!, $childId: ID!) {
  RemoveSchoolManagers(from: { id: $childId }, to: { schoolId: $parentId }) {
    from {
      id
    }
    to {
      schoolId
    }
  }
}
